using Avalonia.Controls;

namespace App.Ui.Views;

/// <summary>The run-time graph options dialog. Logic lives in its view model.</summary>
public sealed partial class RunTimeGraphOptionsWindow : Window
{
    public RunTimeGraphOptionsWindow() => InitializeComponent();
}
